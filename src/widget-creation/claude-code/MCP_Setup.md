# MCP Setup

## Add Kentico Docs MCP server

Run the following command in terminal to add the Kentico Docs MCP server to your Claude MCP configuration:

```bash
claude mcp add-json kentico.docs.mcp '{"type": "http", "url": "https://docs.kentico.com/mcp"}'
```
